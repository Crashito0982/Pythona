;WITH base AS (
  SELECT
    TRY_CONVERT(datetime2(0), FECHA) AS FECHA_DT,  -- podés subir precisión si querés
    FIRMA_NAME,
    ACTIVADO
  FROM dbo.CORPDW_CRM_LEGIT_PRODUCTOS_A_ACTIVAR_NEW
)
SELECT TOP 10 *
FROM base
WHERE FECHA_DT IS NULL
  AND FECHA IS NOT NULL;
